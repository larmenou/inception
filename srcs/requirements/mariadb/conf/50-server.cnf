[server]
init_file = /etc/mysql/init.sql

[mysqld]
user			= root
pid-file		= /run/mysqld/mysqld.pid
basedir			= /usr
datadir 		= /var/lib/mysql
tmpdir			= /tmp

socket  = /run/mysqld/mysqld.sock
bind_address = mariadb
port = 3306